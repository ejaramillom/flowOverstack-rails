<div class="container">
  <div class="row">
    <div class="col-lg-12">
        <div class="table-responsive">
          <table class="table ">
            <!-- <thead>
              <tr>
                <th>Votos</th>
                <th>Respuestas</th>
                <th>Email</th>
              </tr>
            </thead> -->
            <tbody>
              <% @posts.each do |post| %>
              <tr>
                <td>
                  <div class="votes"><%= pluralize(post.votes.size, 'Vote') %></div>
                </td>
                <td>
                  <div class="votes"><%= pluralize(post.answers.size, 'Answer') %></div>
                </td>
                <td>
                  <div class="name">
                    <h4><span><%= link_to post.name, post_path(post), class: 'title' %></span></h4>
                    <p class="asked">Preguntó <%= post.user.email %> hace <%= time_ago_in_words(post.created_at)%> </p>
                  </div>
                </td>
                <td >
                <div class="votes">
                <% if user_signed_in? %>
                  <% if current_user.id == post.user_id %>
                    <%= form_tag(post_path(post.id), method: :delete, data: { confirm: '¿De verdad deseas borrarlo?' }) do%>
                      <%= button_tag( class: "btn icon-t pull-right") do %>
                        <span class="glyphicon glyphicon-trash"></span>
                      <% end %>
                    <% end %>
                    <%= link_to edit_post_path(post.id), class: "btn icon-t pull-right" do  %>
                      <span class="glyphicon glyphicon-pencil"></span>
                    <% end %>
                  <% end %>
                <% end %>
                </div>
                </td>
              </tr>
              <% end %>
            </tbody>
          </table>
      </div>
    </div>
  </div>
</div>
